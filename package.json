{
  "name": "tripbite",
  "version": "0.1.0",
  "description": "食途 TripBite — 旅行美食规划 Agent (Claude Agent SDK + MCP)",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --import tsx src/index.ts",
    "smoke": "node --env-file=.env --import tsx src/smoke.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["agent", "mcp", "travel", "claude-agent-sdk"],
  "license": "MIT",
  "packageManager": "pnpm@10.28.0",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.231",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
